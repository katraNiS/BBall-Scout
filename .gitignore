# Data (large, regenerated locally via pipeline/fetch_nba_data.py — see CLAUDE.md)
data/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Node / frontend
node_modules/
frontend/node_modules/
frontend/dist/
*.tsbuildinfo

# Electron build output
release/
backend-dist/

# Editor / run-tool temp files
tempCodeRunnerFile.py

# Local env overrides
.env
.env.local

# Claude Code local config (per-machine permissions + dev-server launch config)
.claude/

# Prospect store (user data — resolved via PROSPECTMATCH_DATA_DIR, dev fallback here)
.prospectmatch-data/
